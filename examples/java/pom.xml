<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.wickra</groupId>
  <artifactId>wickra-backtest-examples</artifactId>
  <version>0.1.3</version>
  <packaging>jar</packaging>

  <name>wickra-backtest examples (Java)</name>
  <description>Runnable Java examples for the wickra-backtest engine.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>22</maven.compiler.release>
    <!-- The native C-ABI library lives in the Cargo target dir. Build it with:
         cargo build -p wickra-backtest-c -->
    <native.lib.dir>${project.basedir}/../../target/debug</native.lib.dir>
    <exec.mainClass>org.wickra.backtest.examples.Backtest</exec.mainClass>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.wickra</groupId>
      <artifactId>wickra-backtest</artifactId>
      <version>0.1.3</version>
    </dependency>
    <dependency>
      <groupId>org.json</groupId>
      <artifactId>json</artifactId>
      <version>20260814</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
      </plugin>

      <!--
        Run the example in a forked JVM with native access granted and the C ABI
        on the loader path:
          mvn -f examples/java compile exec:exec
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.6.3</version>
        <configuration>
          <executable>${java.home}/bin/java</executable>
          <arguments>
            <argument>--enable-native-access=ALL-UNNAMED</argument>
            <argument>-Djava.library.path=${native.lib.dir}</argument>
            <argument>-classpath</argument>
            <classpath/>
            <argument>${exec.mainClass}</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
