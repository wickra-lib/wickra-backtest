/* Generated by CMake from golden/requests/ -- do not edit.
 *
 * The list lives here rather than in golden.c so that adding a fixture to the
 * shared corpus is enough to have the C ABI assert against it too. */
#ifndef WICKRA_GOLDEN_CASES_H
#define WICKRA_GOLDEN_CASES_H

#include <stddef.h>

static const char *const CASES[] = {
@WICKRA_GOLDEN_CASE_LINES@};
static const size_t NUM_CASES = sizeof(CASES) / sizeof(CASES[0]);

#endif /* WICKRA_GOLDEN_CASES_H */
