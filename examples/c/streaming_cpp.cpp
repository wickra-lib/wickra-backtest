// C++ build of the streaming example, from the same source as the C target. It
// covers the raw handle-and-free calling style under a C++ compiler; cpp_smoke
// covers the same run through the RAII wrapper.
#include "streaming.c"
