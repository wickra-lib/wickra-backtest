// C++ build of the minimal C example. The C ABI header is `extern "C"`, so the
// exact same source compiles under a C++ compiler -- a claim the READMEs make,
// which is worth compiling rather than asserting.
#include "example.c"
